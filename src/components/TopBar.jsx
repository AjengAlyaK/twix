// eslint-disable-next-line no-unused-vars
import React from 'react';
import { Topbar, Title } from '../styles/styledTopBar';

const TopBar = () => {
    return (
        <Topbar>
            <Title>TwiX</Title>
        </Topbar>
    );
};

export default TopBar;